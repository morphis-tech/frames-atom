describe("Frames Atom package", function()
{
	describe("just an example", function()
	{
		it("some example", function()
		{
			runs(function()
			{
				var a = 12;
				expect(a).toEqual(12);
			});
		});
	});
});
